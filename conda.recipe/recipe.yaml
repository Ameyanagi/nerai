context:
  version: "0.0.0"

package:
  name: mojo-nerai
  version: ${{ version }}

source:
  path: ..

build:
  number: 0
  script:
    - mojo precompile src/nerai -o ${{ PREFIX }}/lib/mojo/nerai.mojoc

requirements:
  build:
    - mojo-compiler =1.0.0
  host:
    - mojo-compiler =1.0.0
  run:
    - ${{ pin_compatible('mojo-compiler') }}

tests:
  - script:
      - mojo run test_package.mojo
    files:
      recipe:
        - test_package.mojo

about:
  homepage: https://github.com/Ameyanagi/nerai
  repository: https://github.com/Ameyanagi/nerai
  license: MIT OR Apache-2.0
  license_file:
    - LICENSE-MIT
    - LICENSE-APACHE
  summary: Optimization and nonlinear least squares for Mojo.

extra:
  maintainers:
    - Ameyanagi
